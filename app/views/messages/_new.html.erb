<%=render partial: "messages"%>

<%= form_for :message, :url => "/messages?message[reciever_id]=#{params[:id]}" do |f| %>
  <%= f.label :message %>:
  <%= f.text_area :content %><br />

  <%= f.submit %>
<% end %>
